// Ambient .d.ts barrel for SEO-related types (type-only surface) to allow lightweight imports
// when runtime helpers (like validateSEOBase) are not required.

export type { SEOBase, SEOProps, BreadcrumbItem, OGType, TwitterCardType } from './seo';
